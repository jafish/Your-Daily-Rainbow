Key Questions:
	- How does video behave differently from images? (even though the `image` function is the one that displays the actual video)
	- Can I achieve a form of masking over the displayed video? And if so, can that masking be animated?
	- Can I get access to the pixels array of a video?
